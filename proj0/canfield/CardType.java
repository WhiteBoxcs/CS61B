/**
 * @author MadcowD
 */
package canfield;

/**
 * @author MadcowD Specifies the different types of cards (namely as a result
 *         of anti instance of graders)
 */
enum CardType {
    WASTE, RESERVE, TABLEAU_BASE, TABLEAU_HEAD, FOUNDATION, STOCK,
    TABLEAU_NORM, TABLEAU_EMPTY
}
