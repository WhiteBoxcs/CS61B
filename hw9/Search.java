import java.util.List;

/** Graph searching utlity.
 *  @author
 */
public class Search {

    /** Return a shortest path from vertex START to vertex END in G.
     *  The edge labels of G are the lengths of the edges (all
     *  non-negative).  The vertex labels are 2-element arrays
     *  containing the coordinates of the vertices.  The Euclidean
     *  distance between vertices joined by an edge is guaranteed to
     *  be less than or equal to the length of the edge.  The path is
     *  returned as a list of the vertices along the path, with the
     *  first being START and the last being LAST. */
    public static List<Integer> shortestPath(Graph<Double[], Double> G,
                                             int start, int end) {
        return null; // REPLACE WITH SOLUTION
    }

}

